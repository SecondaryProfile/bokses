name: bokses_server
description: API server for self-hosted Bokses — accounts, boxes and items in Postgres.
publish_to: 'none'
version: 0.23.0

environment:
  sdk: ^3.9.0

dependencies:
  hashlib: ^2.4.2
  postgres: ^3.5.12
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  uuid: ^4.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.32.0
