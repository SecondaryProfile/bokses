// Stub used when dart:html isn't available (the Dart VM, e.g. `flutter test`).
// The real implementation lives in web_download_web.dart.
void triggerWebDownload(String content, String filename,
    {String mimeType = 'application/json'}) {}
